window.SIDEBAR_ITEMS = {"struct":[["AssetFeeAsExistentialDepositMultiplier","A `ChargeFeeInFungibles` implementation that converts the output of a given WeightToFee implementation an amount charged in a particular assetId from pallet-assets"],["ConcreteNativeAssetFrom","Accepts an asset if it is a native asset from a particular `MultiLocation`."],["DenyReserveTransferToRelayChain",""],["DenyThenTry","Deny executing the XCM if it matches any of the Deny filter regardless of anything else. If it passes the Deny, and matches one of the Allow cases then it is let through."]]};